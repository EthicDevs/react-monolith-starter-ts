import type { AppThemeScheme } from "./types";

type Const = {
  APP_NAME: string;
  DEFAULT_THEME_SCHEME: AppThemeScheme;
};

export const Const: Const = {
  APP_NAME: "React Monolith App",
  DEFAULT_THEME_SCHEME: "dark",
};
